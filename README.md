A Claude hook by mechanism that shipped as plugin to be able to have control on the subagent workflows. Before any subagent runs, it makes Claude show you the full list in the terminal with what each one does, and you pick the model for each one separately. So you don't burn opus tokens on simple tasks just because the subagent inherited the model.